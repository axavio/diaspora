
.span-24
  = render :partial => 'admins/admin_bar.haml'
%br
%br

.span-24.last
  %h1
    = "New Users Per Week"
    
  %h2
    = "(Current server date is: #{Time.now.to_date}) "
  
  %ul
    - @created_users_by_week.keys.sort.each do |k|
      %li
        = "Users Created During Week Starting on #{k} , #{@created_users_by_week[k]}"
